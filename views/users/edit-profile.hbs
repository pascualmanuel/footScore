<div class="container">
    <div class="row justify-content-center">

        <div class="col-md-6">
            <h1>Modificación del perfil de usuario</h1>
            <br>
            <form action="/registro" method="POST">
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label for="username">Nuevo nombre de Usuario</label>
                        <input type="text" class="form-control" id="username" name="username"
                            placeholder="{{username}}">
                    </div>
                    <div class="form-group col-md-6">
                        <label for="email">Email</label>
                        <input type="text" class="form-control" id="email" name="email" placeholder="{{email}}">
                    </div>
                </div>

                <div class="form-group">
                    <label for="img">Imagen</label>
                    <img src="#" alt="imagen del perfil">
                    <input type="text" class="form-control" id="img" name="img" placeholder="Image Url">
                </div>
                {{!-- <div class="form-row">
                    <h2>Selecciona tu equipo favorito</h2>
                    <div class="form-group col-md-12">
                        <label for="country">Pais:</label>
                        <select name="country" class="form-control" id="country">
                            <option>Seleccionar</option>
                            {{#each allParks}}
                            <option value="{{_id}}">{{name}}</option>
                            {{/each}}
                        </select>
                    </div>
                </div> --}}
                {{!-- <div class="form-row">
                    <div class="form-group col-md-6">
                        <label for="league">Liga:</label>
                        <select name="league" class="form-control" id="league">
                            <option>Seleccionar</option>
                            {{#each allParks}}
                            <option value="{{_id}}">{{name}}</option>
                            {{/each}}
                        </select>
                    </div> --}}
                    {{!-- <div class="form-group col-md-6">
                        <label for="team">Equipo:</label>
                        <select name="team" class="form-control" id="team">
                            <option>Seleccionar</option>
                            {{#each allParks}}
                            <option value="{{_id}}">{{name}}</option>
                            {{/each}}
                        </select>
                    </div>
                </div> --}}

                <div class="form-group">
                    <div class="form-check">
                        <label class="form-check-label" for="journalist">
                            <input class="form-check-input" type="checkbox" value="true" name="journalist">
                            <p>Si eres periodista y quieres ayudar a la Familia FootScore, marca este check</p>
                        </label>
                    </div>
                </div>
                <button type="submit" class="btn btn-dark btn-block"> Actualizar perfil </button>
            </form>

        </div>
    </div>
</div>